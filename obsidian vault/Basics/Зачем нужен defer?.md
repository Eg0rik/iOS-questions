- defer откладывает выполнение блока кода до выхода из области видимости. 
- Это полезно для освобождения ресурсов или выполнения завершающих действий, например:
```swift 
func processFile() {
    let file = openFile()
    
    defer {
        closeFile(file)
    }
    // Работа с файлом
}
```